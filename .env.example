# Ariadne currently requires no environment variables.
#
# Add a variable here only when committed code consumes it. Never place real
# secrets, access tokens, personal data, or device identifiers in this file.

